[build]
  command = "npm run build" # Adjust if you're using a different build command
  publish = "dist" # Change this to "build" if you're using React

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
